//function Declaration

function addFn(num) {
    console.log(num * num);
}

addFn(10)


//Function Expression - When you store a function inside a variable it is a called function variable

const expFn = (num) => {
    console.log(num*num);
 
}

expFn(5)